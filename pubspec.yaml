name: console_tools
description: A consolle logger class, have custom colors, and text styles already defined.
version: 1.0.1
homepage: https://github.com/ymrabti/console_tools.git

environment:
  sdk: ">=3.1.0 <4.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
